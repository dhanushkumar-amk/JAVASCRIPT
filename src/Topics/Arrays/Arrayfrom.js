/** Arrays.prototype.from() */
/** it is used to convert the particular object into an array  */

const name = 'dhanushkumar';

const result = Array.from(name);
console.log(result);

/** output :
 * 
 * [
  'd', 'h', 'a', 'n',
  'u', 's', 'h', 'k',
  'u', 'm', 'a', 'r'
]
 */

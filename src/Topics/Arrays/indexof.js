/** Arrays.prototype.indexof() */
/** Returns the index of the first occurrence of a value in an array, or -1 if it is not present. */
const string = ['apple', 'orange', 'banana'];

const result1 = string.indexOf('apple');
const result2 = string.indexOf('graphs');
console.log(result1);
console.log(result2);

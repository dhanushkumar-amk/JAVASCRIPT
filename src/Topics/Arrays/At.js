/* ! Array.prototype.at();*/const characters = ['a', 'b', 'c'];

console.log(characters[1]);

/** it gives the index value at the position */
console.log(characters.at(2));

console.log(characters.at(-1));
/** if you put -1 then it goes the last element of an array */

/** Arrays.prototype.includes() *//** Determines whether an array includes a certain element, returning true or false as appropriate. */

const numbers = [1, 2, 3, 4, 5, 6, 7, 8];

const result = numbers.includes(3);

console.log(result);

// return boolean value

const string = ['apple', 'orange', 'banana'];

const result1 = string.includes('apple');
const result2 = string.includes('graphs');
console.log(result1);
console.log(result2);

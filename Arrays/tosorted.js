/* ! Array.prototype.toSorted();*/
const sorted = [1, 2, 3, 4, 5, 6, 7, 8];

const result = sorted.toSorted();
console.log(result);

// to sorted method takes the copy of the original array and return the copy array with sorted order
// here  the original array is still unsorted

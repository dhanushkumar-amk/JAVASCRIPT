/** Arrays.toReversed() * */const string = ['apple', 'orange', 'banana'];

console.log(string.toReversed());

// it take first shallow copy of the original array and return the copied array with  reversed order
